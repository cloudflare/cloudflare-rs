all: &all
  test:
    builddeps:
      - rust=1.31.0-1
      - cargo-to-teamcity
      - libssl-dev
      - pkg-config
    pre-cache:
      - export OPENSSL_DIR=/usr/include/openssl
    post-cache:
      - cargo test | cargo-to-teamcity
jessie: *all
stretch: *all
